name: Apply refund approval UI

on:
  push:
    branches: [main]
    paths:
      - '.github/workflows/apply-refund-approval-ui.yml'

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Patch SalesPage
        run: python3 .github/scripts/patch_refund_approval_ui.py
      - name: Commit patch
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add src/features/trade/pages/SalesPage.tsx
          git commit -m "feat: let cashiers request manager approval for refunds"
          git push
